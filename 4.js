// Membuat progam untuk menampilkan string "football" ketika mengakses array my[0].very["best"][1].sport.is

const my = [{ very: { best: ['l', { sport: { is: 'football' } }] } }, 's'];

console.log(my[0].very['best'][1].sport.is);
